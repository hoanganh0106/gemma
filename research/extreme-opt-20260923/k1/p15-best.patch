diff --git a/research/extreme-opt-20260923/baseline/src/device/sliding/qkv_head_local.rs b/research/extreme-opt-20260923/k1/p15-fused-rope-head-broadcast/src/device/sliding/qkv_head_local.rs
index f0ca981..80cdebc 100644
--- a/research/extreme-opt-20260923/baseline/src/device/sliding/qkv_head_local.rs
+++ b/research/extreme-opt-20260923/k1/p15-fused-rope-head-broadcast/src/device/sliding/qkv_head_local.rs
@@ -55,22 +55,31 @@ pub(crate) fn project_query(
     // Qs is flattened [Ns, Gs, Ds]. This only relabels the same wire order.
     let rows: DmTensor<bf16, Chip, HeadCluster, m![Ns % 4, Gs, Ds / 8], m![Ds % 8]> =
         unsafe { scaled.reshape() };
-    // Each 32-slice group owns one query head. Gather through the switch,
-    // leaving the DMA context available for K/V weight transfers.
-    let heads: DmTensor<bf16, Chip, HeadCluster, m![Ns % 4, Gs, Dummy256 % 32], m![Ds]> = device.main
+    let weight_scale: HbmTensorView<'_, bf16, Chip, m![Ns, Gs, Ds]> =
+        unsafe { weight_scale.view().reshape() };
+    let weight_scale: QueryHeadTensor = weight_scale.to_dm(&mut device.tdma);
+    let scale_vrf: VrfTensor<f32, Chip, HeadCluster, QueryHeads, m![Ds]> = device.sub
+        .begin(weight_scale.view())
+        .fetch::<m![Ds / 16], m![Ds % 16]>()
+        .fetch_cast::<f32>()
+        .collect::<m![Ds / 8], m![Ds % 8]>()
+        .to_vrf();
+    // Fuse head assembly and channel scaling; keep the existing BF16 boundary.
+    device.main
         .begin(rows.view())
         .fetch::<m![1], m![Ds % 8]>()
-        .switch::<m![Ns % 4, Gs, Dummy256 % 32], m![Ds / 8]>(SwitchConfig::Broadcast1 { slice1: 32, slice0: 1 })
-        .collect::<m![Ds / 8], m![Ds % 8 # 16]>()
+        .fetch_cast::<f32>()
+        .switch::<QueryHeads, m![Ds / 8]>(SwitchConfig::Broadcast1 { slice1: 32, slice0: 1 })
+        .collect::<m![Ds / 8], m![Ds % 8]>()
+        .vector_init()
+        .vector_intra_slice_tag(TagMode::Zero)
+        .vector_narrow_split::<m![Ds / 4], m![Ds % 4]>()
+        .vector_fp_binary(FpBinaryOp::MulF(FpMulAlu::Mul0), &scale_vrf)
+        .vector_widen_concat::<m![Ds / 8], m![Ds % 8]>()
+        .vector_final()
+        .cast::<bf16, m![Ds % 8 # 16]>()
         .commit_trim::<m![Ds % 8]>()
-        .commit();
-    // The switch initialized every replica. Keep the first replica of each head.
-    let heads: QueryHeadTensor = unsafe { heads.reshape() };
-    let weight_scale: HbmTensorView<'_, bf16, Chip, m![Ns, Gs, Ds]> =
-        unsafe { weight_scale.view().reshape() };
-    let weight_scale: DmTensor<bf16, Chip, HeadCluster, QueryHeads, m![Ds]> =
-        weight_scale.to_dm(&mut device.tdma);
-    scale_head(device, &heads, &weight_scale)
+        .commit()
 }
 
 
@@ -109,21 +118,31 @@ fn project_one_kv_matrix(
     // Ps is flattened [Ns, Ds]; all 256 row slices contain valid data.
     let rows: DmTensor<bf16, Chip, HeadCluster, m![Ns % 4, Ds / 4], m![Ds % 4]> =
         unsafe { scaled.reshape() };
-    // Each 64-slice group owns one key/value head.
-    let heads: DmTensor<bf16, Chip, HeadCluster, m![Ns % 4, Dummy256 % 64], m![Ds]> = device.main
+    let weight_scale: HbmTensorView<'_, bf16, Chip, m![Ns, Ds]> =
+        unsafe { weight_scale.view().reshape() };
+    let weight_scale: KvHeadTensor = weight_scale.to_dm(&mut device.tdma);
+    let scale_vrf: VrfTensor<f32, Chip, HeadCluster, KvHeads, m![Ds]> = device.sub
+        .begin(weight_scale.view())
+        .fetch::<m![Ds / 16], m![Ds % 16]>()
+        .fetch_cast::<f32>()
+        .collect::<m![Ds / 8], m![Ds % 8]>()
+        .to_vrf();
+    // Every head replica receives the same scaled row values as the unfused path.
+    device.main
         .begin(rows.view())
         .fetch::<m![1], m![Ds % 4]>()
-        .switch::<m![Ns % 4, Dummy256 % 64], m![Ds / 4]>(SwitchConfig::Broadcast1 { slice1: 64, slice0: 1 })
-        .collect::<m![Ds / 4], m![Ds % 4 # 16]>()
+        .fetch_cast::<f32>()
+        .switch::<KvHeads, m![Ds / 4]>(SwitchConfig::Broadcast1 { slice1: 64, slice0: 1 })
+        .collect::<m![Ds / 4], m![Ds % 4 # 8]>()
+        .vector_init()
+        .vector_intra_slice_tag(TagMode::Zero)
+        .vector_narrow_trim::<m![Ds % 4]>()
+        .vector_fp_binary(FpBinaryOp::MulF(FpMulAlu::Mul0), &scale_vrf)
+        .vector_widen_pad::<m![Ds % 4 # 8]>()
+        .vector_final()
+        .cast::<bf16, m![Ds % 4 # 8 # 16]>()
         .commit_trim::<m![Ds % 4]>()
-        .commit();
-    // Only hide already initialized replicas; padding never creates data.
-    let heads: KvHeadTensor = unsafe { heads.reshape() };
-    let weight_scale: HbmTensorView<'_, bf16, Chip, m![Ns, Ds]> =
-        unsafe { weight_scale.view().reshape() };
-    let weight_scale: DmTensor<bf16, Chip, HeadCluster, KvHeads, m![Ds]> =
-        weight_scale.to_dm(&mut device.tdma);
-    scale_head(device, &heads, &weight_scale)
+        .commit()
 }
 
 
@@ -163,7 +182,7 @@ fn root_mean_square<S: M>(
         .vector_final()
         .commit_trim::<m![1 # 8]>()
         .commit();
-    let rms: DmTensor<f32, Chip, HeadCluster, S, m![1 # 8]> = device.main
+    device.main
         .begin(mean_square.view())
         .fetch::<m![1], m![1 # 8]>()
         .collect::<m![1], m![1 # 8]>()
@@ -173,13 +192,7 @@ fn root_mean_square<S: M>(
         .vector_fp_unary(FpUnaryOp::Sqrt)
         .vector_widen_pad::<m![1 # 8]>()
         .vector_final()
-        .commit_trim::<m![1 # 8]>()
-        .commit();
-    device.sub
-        .begin(rms.view())
-        .fetch::<m![1], m![1 # 8]>()
-        .collect::<m![1], m![1 # 8]>()
-        .to_vrf()
+        .to_vrf(&mut device.sub)
 }
 
 pub(crate) fn normalize_weighted<S: M>(
@@ -269,7 +282,7 @@ fn rope_one<S: M>(
         .commit_trim::<m![Ds = 128 % 16]>()
         .commit_view(rotated.view_mut().tile::<m![Ds], 128, m![Ds = 128 #{!} 256]>(0));
 
-    let sin_product: DmTensor<f32, Chip, HeadCluster, S, m![Ds]> = device.main
+    let sin_product_vrf: VrfTensor<f32, Chip, HeadCluster, S, m![Ds]> = device.main
         .begin(rotated.view())
         .fetch::<m![Ds / 16], m![Ds % 16]>()
         .fetch_cast::<f32>()
@@ -280,13 +293,7 @@ fn rope_one<S: M>(
         .vector_fp_binary(FpBinaryOp::MulF(FpMulAlu::Mul1), &sin_vrf)
         .vector_widen_concat::<m![Ds / 8], m![Ds % 8]>()
         .vector_final()
-        .commit_trim::<m![Ds % 8]>()
-        .commit();
-    let sin_product_vrf: VrfTensor<f32, Chip, HeadCluster, S, m![Ds]> = device.sub
-        .begin(sin_product.view())
-        .fetch::<m![Ds / 8], m![Ds % 8]>()
-        .collect::<m![Ds / 8], m![Ds % 8]>()
-        .to_vrf();
+        .to_vrf(&mut device.sub);
     // The cosine product feeds the final addition without a DM round trip.
     device.main
         .begin(x.view())
@@ -313,18 +320,15 @@ pub(crate) fn apply_rope(
     cos: &HbmTensor<bf16, Chip, m![E, Ds]>,
     sin: &HbmTensor<bf16, Chip, m![E, Ds]>,
 ) -> (QueryHeadTensor, KvHeadTensor) {
-    use crate::device::layout::{Cluster, Slice};
-
-    // A scalar indirect gather reads one table row. Keep that row free of head
-    // axes, then use ordinary DMA to explicitly populate every head replica.
-    let cos_row: DmTensor<bf16, Chip, Cluster, Slice, m![Ds]> =
+    // These are true initialized broadcast distributions, not padded axes.
+    // A scalar offset selects one row, requested at both clusters and each live query-head slice.
+    let cos_row: DmTensor<bf16, Chip, m![2], m![8, 1 # 32], m![Ds]> =
         cos.dma_gather_scaled(rope_offset);
-    let sin_row: DmTensor<bf16, Chip, Cluster, Slice, m![Ds]> =
+    let sin_row: DmTensor<bf16, Chip, m![2], m![8, 1 # 32], m![Ds]> =
         sin.dma_gather_scaled(rope_offset);
-    let cos_row: HbmTensor<bf16, Chip, m![Ds]> = cos_row.to_hbm(&mut device.tdma);
-    let sin_row: HbmTensor<bf16, Chip, m![Ds]> = sin_row.to_hbm(&mut device.tdma);
-    let q_cos: QueryHeadTensor = cos_row.to_dm(&mut device.tdma);
-    let q_sin: QueryHeadTensor = sin_row.to_dm(&mut device.tdma);
+    // Relabel already broadcast copies; no element order is changed.
+    let q_cos: QueryHeadTensor = unsafe { cos_row.reshape() };
+    let q_sin: QueryHeadTensor = unsafe { sin_row.reshape() };
     let q = rope_one(device, q, &q_cos, &q_sin);
     // Both Gs copies were initialized by DMA; retain Gs=0 for each key head.
     let cos: KvHeadTensor = unsafe { q_cos.reshape() };
@@ -399,7 +403,7 @@ pub(crate) fn normalize_native_input(
         .vector_final()
         .commit_trim::<m![1 # 8]>()
         .commit();
-    let rms: DmTensor<f32, Chip, QkvDualQueryCluster, Copies, m![1 # 8]> = device.main
+    let rms: VrfTensor<f32, Chip, QkvDualQueryCluster, Copies, m![1 # 8]> = device.main
         .begin(mean.view())
         .fetch::<m![1], m![1 # 8]>()
         .collect::<m![1], m![1 # 8]>()
@@ -409,16 +413,9 @@ pub(crate) fn normalize_native_input(
         .vector_fp_unary(FpUnaryOp::Sqrt)
         .vector_widen_pad::<m![1 # 8]>()
         .vector_final()
-        .commit_trim::<m![1 # 8]>()
-        .commit();
-    // Retain only the shard lanes from the initialized all-reduce replicas.
-    let rms: DmTensor<f32, Chip, QkvDualQueryCluster, Shards, m![1 # 8]> =
+        .to_vrf(&mut device.sub);
+    let rms_vrf: VrfTensor<f32, Chip, QkvDualQueryCluster, Shards, m![1 # 8]> =
         unsafe { rms.reshape() };
-    let rms_vrf: VrfTensor<f32, Chip, QkvDualQueryCluster, Shards, m![1 # 8]> = device.sub
-        .begin(rms.view())
-        .fetch::<m![1], m![1 # 8]>()
-        .collect::<m![1], m![1 # 8]>()
-        .to_vrf();
     let weight_vrf: VrfTensor<f32, Chip, QkvDualQueryCluster, Shards, m![H % 120]> = device.sub
         .begin(weight.view())
         .fetch::<m![H / 8 % 15], m![H % 8]>()
