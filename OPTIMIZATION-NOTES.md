# Stage 1 최적화 기록

측정 환경: 로컬 RNGD (npu0~3, 펌웨어 2026.3.0), `cargo-furiosa-opt` 0.6.0,
`furiosa-opt-std` 0.6.0, nightly-2026-05-01. 정확도는 `tests/test_kernels.rs` 로 매번 확인했다.

## 결과

| 커널 | 기준선 | 현재 | 배속 |
|---|---:|---:|---:|
| `ops::sliding_project_qkv` | 265,101 | 149,393 | 1.78× |
| `ops::sliding_attention_output` | 435,551 | 62,931 | 6.92× |
| `ops::decoder_feedforward` | 3,720,750 | 558,900 | 6.66× |
| **점수 (기하평균)** | | | **약 4.3×** |

채점 서버 기준선(250,514 / 404,633 / 3,703,473)은 우리 로컬 기준선과 4~7% 안에서 일치하므로
로컬에서 잰 배속이 그대로 전이된다고 봐도 된다.

**측정 주의.** `TUC_PROFILE_LEVEL=info` 가 주는 on-device 사이클은 같은 바이너리에서도
20~35% 흔들린다(같은 FFN 이 558,900 ~ 875,707 로 나온 적이 있다). 한 번 재고 판단하면
반드시 틀린다 — 한 번은 "개선했는데 느려졌다"고 오판할 뻔했다. `scripts/measure.sh` 로 여러 번
돌려 **최솟값**을 쓴다. 빠른 반복에는 컴파일만 하면 나오는 스케줄 makespan 을 쓰고(실측/makespan
비가 1.9~2.2 로 일정), 확정은 실측으로 한다.

## 무엇을 바꿨나

바꾼 파일은 채점 대상인 `src/ops.rs` 의 함수 본문과 `src/device/` 뿐이다.
`#[device]` 함수의 이름과 시그니처는 그대로다.

### 1. 역양자화한 가중치를 DM 에 펼치지 않는다

기준 코드는 f8 가중치를 통째로 bf16 으로 바꿔 DM 에 쓴 다음 그걸 다시 읽어 곱했다.
`fetch_table_lookup` 은 컨트랙션의 fetch 에 바로 붙일 수 있으므로(`PositionFetchTableLookup`
→ `collect` → `contract_outer`), 그 패스와 두 배 넓은 사본이 통째로 사라진다.
`TableLookup<bf16> for f8e4m3` 는 정확 변환이라 **수치가 하나도 바뀌지 않는다.**

MLP 의 f4 가중치도 같은 요령으로 `fetch_table_lookup::<f8e4m3>()` + `fetch_cast::<f32>()` 를
스케일 곱 패스의 fetch 에 붙여, 행렬 전체를 f8 로 펼치던 패스를 없앴다.

### 2. 쓰지도 않는 곳까지 하던 브로드캐스트를 없앴다

`sliding_attention_output` 은 x 를 Switch 링으로 256 슬라이스에 복사했지만
`output_partial` 은 그중 32개만 읽었다. 링은 목적지 하나당 한 플릿씩 옮기므로 비용이
목적지 수에 비례한다. HBM 에서 필요한 슬라이스로 바로 DMA 복제하면 66,311 → 6,803 이 된다.

`sliding_project_qkv` 은 RMSNorm 이 이미 H 를 여덟 조각으로 나눠 갖고 있다는 점을 쓴다.
그 조각들을 하나로 모았다가 다시 전체를 뿌리는 대신, 조각난 채로 32개 행 묶음에 복제하면
복사량이 8분의 1이다 (`shared::rmsnorm::normalize_columns`).

### 3. 놀고 있던 슬라이스를 쓴다

`project_output` 은 Qs 를 1024열씩 네 조각으로 잘라 **순서대로** 돌리고 부분합 셋을 더했다.
그동안 224개 슬라이스가 놀았다. 열을 슬라이스에 **펼쳐서** 한 번에 곱하고 슬라이스 간
축약으로 합치면 모든 슬라이스가 일한다. 덤으로 부분합을 bf16 으로 반올림하던 세 번이
사라져 평균 오차가 0.000965 → 0.000583 으로 좋아졌다.

### 4. 칩의 두 번째 클러스터를 쓴다 — 가장 큰 한 방

기준 코드는 `Cluster = m![1 # 2]` 로 **칩의 절반만** 썼다. 두 클러스터가 HBM 을 각자
읽는지부터 쟀다: 가중치를 양쪽에 복제해 바이트를 2배로 늘렸는데 적재 시간이 26,475 로
**똑같았다** → 대역폭이 따로다. 세 커널 모두 DMA 한계였으므로 출력 행을 두 클러스터로
나눴고, 적재 시간이 그대로 반이 됐다. 클러스터를 넘는 값은 칩 공용인 HBM 에서 만난다
(7.5~30 KB, 무시할 수준).

### 5. DMA 와 계산이 겹치게 했다

FFN 은 처음 35만 사이클이 완전히 직렬이었다 — DMA 가 가중치를 싣는 동안 Main 이 놀고,
Main 이 계산하는 동안 DMA 가 놀았다. 원인은 작업 집합이었다: 행렬 하나가 f4(115 KB/슬라이스)와
펼친 f8(230 KB/슬라이스)로 512 KB 중 345 KB 를 차지해 다음 행렬이 들어올 자리가 없었다.
타일 단위로 흘려보내고, up·gate 를 한 루프에서 번갈아 처리하고, 세 패스를 펼쳐
**동시에 흐르는 타일을 6개**로 늘렸다(FFN 285K → 256K).

## 함정으로 배운 것

- **컨트랙션의 두 피연산자는 같은 부동소수 타입이어야 한다** (`ContractionWeight`).
  f8 가중치 × bf16 활성값은 컴파일되지 않는다. 표 조회로 f8 을 bf16 으로 올려서 맞춘다.
- **슬라이스 배치 순서가 DMA 속도를 좌우한다.** `m![H / 120, 1 # 8]`(간격 8)을
  `m![1 # 8, H / 120]`(연속)으로 바꿨더니 같은 가중치 적재가 27,928 → 124,724 이 됐다.
- **DMA 전송 한 건에 약 900 사이클의 고정 비용**이 있다. 15.7 MB 한 번(13,383)과
  1.97 MB 한 번(2,473)을 맞춰 풀면 한계 속도 1,258 B/사이클 + 상수 907 이 나온다.
  타일이 45개면 그 고정 비용만 4만 사이클이다.
- **DM 할당은 슬라이스 256개를 꽉 채워야 한다.** 128개짜리 매핑은 거부된다(패딩은 된다).
- **슬라이스 안 원소는 8바이트 배수**, **collect 패킷은 정확히 32바이트**,
  **commit 패킷은 8/16/24/32바이트**. 행 수를 정할 때 이 셋이 동시에 걸린다.
- **transpose 의 축은 타일 축을 가리켜야 한다.** 40행 타일에서 `m![H / 4 % 10]` 은
  전역 H 기준이라 거부된다 — `m![H % 120 = 40 / 4]` 로 써야 한다.
- **DMA 는 부분 복제를 못 내린다.** 슬라이스 매핑에 다른 실제 축이 남아 있으면
  "while lowering Dma" 로 실패한다. 그 복제는 Switch 엔진으로 해야 한다.
- **`const PASSES = A / B` 의 정수 나눗셈**에 잘려 슬라이스당 60행 중 56행만 계산한 적이
  있다. 틀린 칸이 5.57% 로 나와 잡혔다. 나눠떨어지는지 꼭 확인할 것.
- **`Broadcast1` 은 붙어 있는 슬라이스를 모은다.** 슬라이스 간 축약을 거치면 결과가 한 칸씩
  띄어 놓이므로 `slice0` 으로 보폭을 알려줘야 한다.
- 축 이름은 그 텐서가 실제로 가진 축이어야 한다. x 를 `Qs / 512` 로 쪼개라고 했는데 x 에는
  `Qs` 축이 없어 **모든 슬라이스가 같은 첫 512개**를 받은 적이 있다(`Ns` 로 정정).

## 해봤지만 안 된 것 (다시 하지 말 것)

- 가중치 행렬을 **한 번에 통째로** 싣기: DMA 는 16K 줄었지만 겹침이 무너져 makespan 은
  285K → 303K 로 나빠졌다.
- up/gate 타일을 **12행으로** 키우기: transpose 축 문제로 막히고, 고쳐도 이득 없음.
- `sliding_attention_output` 의 가중치를 **2~3조각으로 흘려보내기**: 스케줄러가 타일을
  겹쳐 주지 않아 37,673 → 39,726 / 40,738 으로 나빠졌다.
- 브로드캐스트를 **Sub 컨텍스트**로 옮기기: 같은 스위치가 62,215 → 234,805 로 3.8배 느리다.

## 다음에 해볼 것

세 커널 모두 여전히 DMA 한계다(88% / 54% / 78%).

1. **전송 효율.** 가중치를 클러스터당 398 B/사이클로 읽고 있는데 연속 통짜 읽기는 600 이다.
   전송 횟수(고정 비용)와 겹침 사이의 균형점을 더 찾아볼 것.
2. `sliding_project_qkv` 의 HBM 왕복 9,814 사이클 — `apply_rope` 를 클러스터 일반화하면
   없앨 수 있다(`normalize_query` 는 이미 일반화되어 있다).
3. `sliding_attention_output` 의 직렬 꼬리 — 투영 뒤 RMSNorm·잔차가 한 줄로 이어진다.
4. FFN 가중치를 f8 상태로 직접 곱하기(16원소 블록 부분합을 남기고 나중에 스케일·합산).
   벡터 작업은 8배 줄지만 지금은 DMA 가 한계라 이득이 제한적일 수 있다.
